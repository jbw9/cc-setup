# <project>

<!-- Drop this in the interview repo root in the first 2 minutes and fill it in
     as you go. A project CLAUDE.md is worth more than every skill combined:
     it stops Claude re-deriving your stack on every turn. -->

## What we're building
<one paragraph: the actual prompt they gave you>

## Constraints from the interviewer
- Time box:
- Must use / must not use:
- What they said they'd evaluate:

## Stack
- Language / runtime:
- Framework:
- Package manager:  <!-- state it: stops Claude guessing npm vs pnpm vs bun -->
- Run:      `<command>`
- Test:     `<command>`
- Lint/build: `<command>`

## Conventions
- Keep it in <N> files until it works; refactor after.
- No new dependencies without asking me.
- Tests only for <the parts that matter>. Don't scaffold a test suite.

## Working agreement
- Small diffs. Show me the plan for anything over ~100 lines.
- After each change, run the test/build command above and paste real output.
- If you're blocked, say so in one line — don't invent an API.

## Status
<!-- keep this updated; it's what survives a compaction -->
- [ ]
- [ ]
